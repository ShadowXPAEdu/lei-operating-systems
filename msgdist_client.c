#include <stdlib.h>
#include <stdio.h>
#include "msgdist.h"

int main(int argc, char* argv[], char** envp) {
    if (argc != 2) {
        printf("You need to specify a username!\nTry %s [username]\n", argv[0]);
        return -1;
    } else {

    }
    return 0;
}
